[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.backends._legacy:_Backend"

[project]
name = "gemma4-e2e-tests"
version = "0.1.0"
description = "End-to-end tests for Gemma4 model API"
requires-python = ">=3.10"
dependencies = [
    "requests>=2.31.0",
]
